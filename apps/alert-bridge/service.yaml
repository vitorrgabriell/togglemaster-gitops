apiVersion: v1
kind: Service
metadata:
  name: alert-bridge
  namespace: monitoring
  labels:
    app: alert-bridge
spec:
  type: ClusterIP
  selector:
    app: alert-bridge
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
